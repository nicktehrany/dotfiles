- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.bashrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
